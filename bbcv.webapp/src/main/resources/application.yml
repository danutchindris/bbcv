spring:
        profiles:
                  active: development

---

spring:
          profiles: development

server:
          port: 8081

ro:
  leje:
    services:
      user: "http://localhost:8083/user"
      role: "http://localhost:8083/role"

---

spring:
          profiles: localtest

server:
          port: 8085

ro:
  leje:
    services:
      user: "http://localhost:8084/user"
      role: "http://localhost:8084/role"
---

spring:
          profiles: production

server:
          port: 8082

ro:
  leje:
    services:
      user: "http://localhost:8083/user"
      role: "http://localhost:8083/role"